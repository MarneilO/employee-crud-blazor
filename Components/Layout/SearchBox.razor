@typeparam TEntity

<input class=" form-control" style="max-width:600px" placeholder="Search..."
    @bind="searhItem" />

<button class="form-control" style="max-width:300px" 
        @onclick="OnSearchClicked">
        Search
</button>

@code {
    [Parameter]
    public List<TEntity> Entity { get; set; }

    [Parameter]
    public EventCallback<string> OnSearch { get; set; }

    private string searhItem;

    private async Task OnSearchClicked()
    {
        await OnSearch.InvokeAsync(searhItem);
    }
}
